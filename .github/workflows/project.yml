name: project
on:
  push:
    branches:
    - init
jobs:
    build:
     runs-on: ubuntu-latest
     steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: 💡 Creando proyecto en git
        run: |
            echo "Creando proyecto en git ${{ github.repository }}"
